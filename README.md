# Git commit hooks for rust projects.

## Usage

1. first create an empty branch `gh-pages` and publish it.
```bash
git checkout --orphan
```

2. Copy the update-docs/post-commit file into your .git/hooks/
  and make it executable
```bash
```




